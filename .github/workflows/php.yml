名称：PHP Composer n non：n Push：n分支：[main]n拉请求：n分支：[main]n nJobs：n Build：n n Run-on：Ubuntu-最新n步：n-use：action/checkout@v2n-name：v2n-name：v2n-name。锁n运行：Composer验证--严格的n n-名称：Cache n-name：Composer包n id：Composer n使用：action/cache@v2 n使用：n路径：供应商n键：${runner.os}-php-${hashFiles(‘**/comer.lock’)}n RESTORE-键：n${runner.os}}-php-n-name：install依赖关系n运行：Composer install-Add-Dist-no-建议n#向Composer.json添加测试脚本，例如：“测试”：“供应商/bin/phpunit”n#Docs：https://getcomposer.org/doc/articles/scripts.md n n#-name：Run test套件n#run：Composer Run-script test
